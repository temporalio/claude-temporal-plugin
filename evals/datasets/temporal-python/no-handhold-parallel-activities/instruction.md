Create a Temporal workflow in Python that runs multiple activities in parallel and combines their results.

Implement this in parallel.py with a workflow named "ParallelWorkflow" that runs three activities concurrently returning "A", "B", "C" and concatenates the results.

Running `uv run python parallel.py` should print `ABC`.

The temporal dev server is available on this system, but you may need to add dependencies to this project.

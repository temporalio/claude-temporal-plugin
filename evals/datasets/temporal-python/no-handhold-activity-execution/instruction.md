Create a calculator service using Temporal in Python that adds two numbers together.

Implement this in calculator.py with a workflow named "CalculatorWorkflow" and a runnable main block that accepts two integers as CLI arguments and prints their sum.

Running `uv run python calculator.py 5 3` should print `8`.

The temporal dev server is available on this system, but you may need to add dependencies to this project.

Create a Temporal workflow in Python that uses a timer to wait before completing.

Implement this in timer.py with a workflow named "TimerWorkflow" that waits for a given number of seconds and reports completion.

Running `uv run python timer.py 2` should print `Completed after 2 seconds`.

The temporal dev server is available on this system, but you may need to add dependencies to this project.
